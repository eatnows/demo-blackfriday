spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-server:3306/demo_blackfriday?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  cassandra:
    keyspace-name: catalog
    port: 9042
    contact-points: cassandra-node-0
    local-datacenter: dcl
    schema-action: create_if_not_exists
